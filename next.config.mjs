/** @type {import('next').NextConfig} */
const nextConfig = {};

/*export const config = {
    matcher: ['/api/contacts/:path*'], // Apply middleware to all API routes
};*/
  
export default nextConfig;
